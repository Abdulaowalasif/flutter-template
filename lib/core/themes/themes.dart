import 'package:flutter/material.dart';

class MyAppThemes {
  static ThemeData get lightThemes => ThemeData(brightness: Brightness.light);

  static ThemeData get darkThemes => ThemeData(brightness: Brightness.dark);
}
